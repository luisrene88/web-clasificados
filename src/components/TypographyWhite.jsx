import {Typography} from "@mui/material";

const TypographyWhite = ({children}) =>{
    return <Typography sx={{color: '#ffffff'}}>
        {children}
    </Typography>
}

export default TypographyWhite;